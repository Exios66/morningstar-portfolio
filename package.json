{
  "scripts": {
    "start": "live-server",
    "build": "echo 'No build step needed, files are ready for deployment'"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5"
  }
}
